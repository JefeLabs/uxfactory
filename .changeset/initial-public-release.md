---
"@uxfactory/spec": minor
"@uxfactory/gate": minor
"@uxfactory/bridge": minor
"@uxfactory/cli": minor
---

Initial public release of the UXFactory engine packages: DesignSpec types + JSON Schema validation (`spec`), the deterministic verification gate (`gate`), the localhost bridge relay (`bridge`), and the `uxfactory` CLI (`cli`).
