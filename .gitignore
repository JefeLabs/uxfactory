node_modules/
dist/
coverage/
# In USER projects, .uxfactory/artifacts/ is the versioned spec inventory and
# should be committed (their .gitignore, their call). In THIS repo the whole
# dir is only ever bridge runtime state + live-smoke debris — never tracked
# (nothing under it has ever been committed; a carve-out only invites
# accidental sweep-ins).
.uxfactory/
*.log
.DS_Store
.idea/
playground/
.superpowers/sdd/

# Local agent/session tooling (never committed)
.claude/
.claude.json
.playwright-mcp/
